latest:
  release: Disable
  beta: Disable
  alpha: v1.3.2-alpha.4

latest_no:
  release: 1
  beta: 1
  alpha: 2

subchannels:
  system:
    - windows
    - linux
  arch:
    - x64
  struct:
    - dir

name_format: SecRandom-[system]-[version]-[arch]-[struct].zip
